# Seedance Local Studio — Python 의존성 목록
# 설치: pip install -r requirements.txt
#
# 의존성 최소화 원칙에 따라 꼭 필요한 패키지만 포함한다.

# Streamlit — 웹 UI 프레임워크
streamlit>=1.35.0

# Requests — HTTP 클라이언트 (Atlas Cloud API 호출)
requests>=2.31.0

# Python-dotenv — .env 파일에서 환경변수 로드
python-dotenv>=1.0.0
